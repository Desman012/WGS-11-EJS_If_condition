<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>contact</title>
    </head>

    <body>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/about">About</a></li>
            <li><a href="/contact">Contact</a></li>
        </ul>
        <h1>Contact</h1>
        <p>Ini adalah contact</p>

        <!-- memakai if ejs untuk mencari apakah data kosong atau tidak -->
        <% if (cont.length < 1) { %>

        <!-- jika data kosong mengeluarkan pesan -->
        <h3><b>error!!</b> data is empty</h3>

        <!-- jika data tidak kosong -->
        <% } else { %>
        <table border="1px solid">
            <tr>
                <th>Nama</th>
                <th>Email</th>
            </tr>
            <!-- menampilkan data dengan ejs foreach -->
            <% cont.forEach(elm => { %>
            <tr>
                <td><%= elm.name %> </td>
                <td><%= elm.email %> </td>
            </tr>
            <% }) %>
            <%} %>
        </table>
    </body>

</html>